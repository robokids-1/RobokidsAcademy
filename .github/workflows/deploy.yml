name: Deploy src to Hostinger

on:
  push:
    branches: [ main ]   # change if your main branch name is different
  workflow_dispatch:    # allows manual triggering from GitHub UI

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy src to public_html via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 82.180.172.250
          username: u360046852.botbees.academy
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          local-dir: src/
          server-dir:  /domains/botbees.academy/public_html/
